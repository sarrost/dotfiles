"〖愛〗vifm commands file

" Create dir
com! mkd !mkdir -p %a
" Create and switch to dir
com! mkcd :mkdir -p %a | cd %a
" Reload
com! reload :write | restart

" FZF intergration
command! fzfcd :execute 'cd' fnameescape(term('dirs_fzf 2>/dev/tty'))

com! zip zip -r %f.zip %f
com! df df -h %m 2> /dev/null
com! diff vim -d %f %F
com! run !! ./%f
com! make !!make %a
com! vgrep vim "+grep %a"

" vim: filetype=vifm
