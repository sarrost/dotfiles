#!/bin/sh
alias alias gan="cd $RICE_ANIME_DIR"
alias gam="cd $RICE_GAMES_DIR"
alias gar="cd $RICE_ARTICLES_DIR"
alias gco="cd $RICE_CODE_DIR"
alias gcf="cd $XDG_CONFIG_HOME"
alias gdc="cd $XDG_DOCUMENTS_DIR"
alias gdt="cd $RICE_DOTFILES_DIR"
alias gdl="cd $XDG_DOWNLOAD_DIR"
alias gli="cd $RICE_LIBRARY_DIR"
alias gls="cd $XDG_DATA_HOME"
alias gmd="cd $RICE_MEDIA_DIR"
alias gmn="cd $RICE_MOUNT_DIR"
alias gmu="cd $XDG_MUSIC_DIR"
alias gno="cd $RICE_NOTES_DIR"
alias gpc="cd $XDG_PICTURES_DIR"
alias grp="cd $RICE_REPOSITORY_DIR"
alias gs="cd $RICE_SCRIPTS_DIR"
alias gvi="cd $XDG_VIDEOS_DIR"
alias gtp="cd $XDG_TEMPLATES_DIR"
c2an(){ cp "$@" "$RICE_ANIME_DIR" }
c2am(){ cp "$@" "$RICE_GAMES_DIR" }
c2ar(){ cp "$@" "$RICE_ARTICLES_DIR" }
c2co(){ cp "$@" "$RICE_CODE_DIR" }
c2cf(){ cp "$@" "$XDG_CONFIG_HOME" }
c2dc(){ cp "$@" "$XDG_DOCUMENTS_DIR" }
c2dt(){ cp "$@" "$RICE_DOTFILES_DIR" }
c2dl(){ cp "$@" "$XDG_DOWNLOAD_DIR" }
c2li(){ cp "$@" "$RICE_LIBRARY_DIR" }
c2ls(){ cp "$@" "$XDG_DATA_HOME" }
c2md(){ cp "$@" "$RICE_MEDIA_DIR" }
c2mn(){ cp "$@" "$RICE_MOUNT_DIR" }
c2mu(){ cp "$@" "$XDG_MUSIC_DIR" }
c2no(){ cp "$@" "$RICE_NOTES_DIR" }
c2pc(){ cp "$@" "$XDG_PICTURES_DIR" }
c2rp(){ cp "$@" "$RICE_REPOSITORY_DIR" }
c2s(){ cp "$@" "$RICE_SCRIPTS_DIR" }
c2vi(){ cp "$@" "$XDG_VIDEOS_DIR" }
c2tp(){ cp "$@" "$XDG_TEMPLATES_DIR" }
m2an(){ mv "$@" "$RICE_ANIME_DIR" }
m2am(){ mv "$@" "$RICE_GAMES_DIR" }
m2ar(){ mv "$@" "$RICE_ARTICLES_DIR" }
m2co(){ mv "$@" "$RICE_CODE_DIR" }
m2cf(){ mv "$@" "$XDG_CONFIG_HOME" }
m2dc(){ mv "$@" "$XDG_DOCUMENTS_DIR" }
m2dt(){ mv "$@" "$RICE_DOTFILES_DIR" }
m2dl(){ mv "$@" "$XDG_DOWNLOAD_DIR" }
m2li(){ mv "$@" "$RICE_LIBRARY_DIR" }
m2ls(){ mv "$@" "$XDG_DATA_HOME" }
m2md(){ mv "$@" "$RICE_MEDIA_DIR" }
m2mn(){ mv "$@" "$RICE_MOUNT_DIR" }
m2mu(){ mv "$@" "$XDG_MUSIC_DIR" }
m2no(){ mv "$@" "$RICE_NOTES_DIR" }
m2pc(){ mv "$@" "$XDG_PICTURES_DIR" }
m2rp(){ mv "$@" "$RICE_REPOSITORY_DIR" }
m2s(){ mv "$@" "$RICE_SCRIPTS_DIR" }
m2vi(){ mv "$@" "$XDG_VIDEOS_DIR" }
m2tp(){ mv "$@" "$XDG_TEMPLATES_DIR" }
